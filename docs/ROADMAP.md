# ARC-Room Roadmap

## v0.1 — Empty intake shell

- Local single-file app
- OBJ/STL upload
- Tactical room renderer
- Slash commands
- Object registry
- Scene export/import

## v0.2 — GLB/GLTF loader

- Add GLB/GLTF support
- Add material/texture manifest
- Add drag-to-place mode
- Add transform gizmo controls

## v0.3 — AI-readable room workflow

- Export compact object-only manifest
- Add command receipts panel
- Add undo/redo from receipts
- Add screenshot capture metadata

## v0.4 — ARC integration

- ARC-Core receipt export
- OmniBinary snapshot manifest
- Deterministic seeded room state
- Replayable command log

## v0.5 — Model testing tools

- Mesh stats panel
- Triangle budget warning
- Auto-scale presets
- Axis/grid calibration
- Collision bounds preview
